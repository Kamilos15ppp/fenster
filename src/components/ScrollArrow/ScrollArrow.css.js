import styled from 'styled-components';

export const Wrapper = styled.div`
  position: fixed;
  right: 3rem;
  bottom: 4rem;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 4rem;
  height: 4rem;
  font-size: 2.5rem;
  color: #fff;
  background-color: rgba(6, 160, 204, 0.85);
  border-radius: 30px;
  cursor: pointer;
  transform: scale(0);
`;
